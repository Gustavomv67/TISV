@model IEnumerable<TISV.Models.Gols>

@{
     
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.falta)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.penalti)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.escanteio)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.falta)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.penalti)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.escanteio)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
                @Html.ActionLink("Details", "Details", new { id=item.id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.id })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
