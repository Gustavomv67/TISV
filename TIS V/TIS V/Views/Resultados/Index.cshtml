@model IEnumerable<TISV.Models.Resultado>

@{
     
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.gols1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.gols2)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.posse1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.posse2)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.gols1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.gols2)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.posse1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.posse2)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
                @Html.ActionLink("Details", "Details", new { id=item.id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.id })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
